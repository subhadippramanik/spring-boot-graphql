type Employee {
  id: Int!
  name: String!
  email: String
}

type Query {
  allEmployees: [Employee]
}

type Mutation {
  addEmployee(id: Int!, name: String!, email: String): Employee
}

schema {
  query: Query
  mutation: Mutation
}
